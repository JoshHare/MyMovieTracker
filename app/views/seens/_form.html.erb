<%= form_with(model: seen) do |form| %>
  <% if seen.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(seen.errors.count, "error") %> prohibited this seen from being saved:</h2>

      <ul>
        <% seen.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
